[project]
name = "tutor-programacion"
version = "0.1.0"
description = "Agente tutor de fundamentos de programación con LLMs (Trabajo 02)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.50",
    "rich>=13.0",
    "python-dotenv>=1.0",
    "fastapi>=0.139.2",
    "uvicorn[standard]>=0.51.0",
]

[project.scripts]
tutor = "tutor.__main__:main"
tutor-web = "tutor.web:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
    "mypy>=1.13",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tutor"]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
# E/W pycodestyle, F pyflakes, I isort, B bugbear, UP pyupgrade,
# N pep8-naming, D docstrings (Google), SIM simplify, RUF reglas de ruff
select = ["E", "W", "F", "I", "B", "UP", "N", "D", "SIM", "RUF"]
pydocstyle.convention = "google"

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["D"]  # los nombres de test ya documentan el comportamiento
# N818 exige sufijo "...Error" en inglés; el dominio usa prefijo en español
# (ErrorLLM, ErrorConfiguracion), consistente en todo el código.
"src/tutor/errores.py" = ["N818"]
"src/tutor/llm.py" = ["N818"]

[tool.mypy]
strict = true
files = ["src"]
mypy_path = "src"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
